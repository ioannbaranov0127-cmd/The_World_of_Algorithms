# -*- coding: utf-8 -*-
"""Тема 7 модуля 2. Функции."""

from __future__ import annotations

from course_data.modules.module_2.project import project_step_for_module_2
from course_data.modules.module_2.project_stage_tasks import STAGE_M2_07

TOPIC: dict = {
    'id': 'm2-t7',
    'num': 7,
    'title': 'Тема 7. Функции',
    'summary': 'def и вызов функции: вынести меню и повторяющийся вывод в отдельные блоки кода.',
    'project_step': project_step_for_module_2(7),
    'theory': {
        'intro': (
            'К версии 1.6 в main.py уже много строк: меню, расчёт, журнал eaten, цикл while. '
            'Один и тот же фрагмент может повторяться — например, вывод меню в начале и после каждой порции. '
            'Функция — это именованный блок кода: описали один раз, вызываем сколько нужно.'
        ),
        'sections': [],
        'visual_blocks': [
            {
                'title': 'Главная мысль темы',
                'body': (
                    'def имя(): — «сохранить рецепт». '
                    'имя() — «приготовить по рецепту». '
                    'Код внутри функции выполняется только при вызове.'
                ),
            },
        ],
        'schemes': [],
        'scheme_gallery_layout': 'stack',
        'scheme_gallery_before': [
            {
                'title': 'Функции в калькуляторе',
                'body': (
                    'На схеме — как работает простая функция без параметров: '
                    'сверху блок def show_menu() с телом функции, '
                    'снизу вызов show_menu() из основной программы. '
                    'Стрелка показывает: программа «перепрыгивает» в функцию, выполняет её строки и возвращается дальше.'
                ),
            },
        ],
        'scheme_gallery': [
            {
                'file': 'Функции_тема_1.svg',
                'num': 1,
                'caption': 'Объявление def и вызов функции',
                'sections_after': [
                    {
                        'title': 'Как читать схему',
                        'body': (
                            'Жёлтый блок def show_menu(): — объявление функции. '
                            'Внутри синего «тела» лежат команды, которые выполнятся при вызове.\n\n'
                            'Блок show_menu() внизу — вызов. '
                            'Python находит функцию с таким именем и выполняет её тело. '
                            'После этого программа идёт к следующей строке после вызова.'
                        ),
                    },
                    {
                        'title': 'Зачем функции, если код уже работает',
                        'body': (
                            'Без функций приходится копировать одинаковые print() и цикл for. '
                            'Если меню нужно показать два раза — код дублируется. '
                            'Функция собирает повторяющиеся строки в одно место: проще читать и исправлять.'
                        ),
                        'code': (
                            '# Без функции — два одинаковых куска\n'
                            'print("Меню:")\n'
                            'print("1. яблоко")\n\n'
                            'print("Меню:")\n'
                            'print("1. яблоко")  # снова то же самое'
                        ),
                    },
                    {
                        'title': 'Объявление: def и имя',
                        'body': (
                            'Запись def show_menu(): читается так: «определить функцию show_menu». '
                            'После def — имя функции (как имя переменной: буквы, цифры, _). '
                            'Скобки () пока пустые — параметры будут в теме 8. '
                            'Двоеточие в конце обязательно — как у if и for.'
                        ),
                        'code': (
                            'def show_menu():\n'
                            '    print("Доступные продукты:")\n'
                            '    print("1. яблоко")\n'
                            '    print("2. банан")'
                        ),
                    },
                    {
                        'title': 'Отступ — тело функции',
                        'body': (
                            'Строки с отступом под def — это тело функции. '
                            'Они не выполняются сразу при чтении файла — только когда функцию вызовут. '
                            'Без отступа Python не поймёт, где заканчивается функция.'
                        ),
                        'code': (
                            'def greet():\n'
                            '    print("Привет!")  # внутри функции\n'
                            'print("Конец файла")  # уже вне функции — выполнится сразу'
                        ),
                    },
                    {
                        'title': 'Вызов функции',
                        'body': (
                            'Чтобы запустить функцию, напишите её имя со скобками: show_menu(). '
                            'Это похоже на eaten.append() — имя и скобки. '
                            'Разница: append — метод списка, show_menu — ваша собственная функция.'
                        ),
                        'code': (
                            'def show_menu():\n'
                            '    print("Меню готово")\n\n'
                            'show_menu()  # вывод: Меню готово\n'
                            'show_menu()  # можно вызвать ещё раз'
                        ),
                    },
                    {
                        'title': 'show_menu() в калькуляторе',
                        'body': (
                            'Вынесите в функцию вывод каталога products — цикл for из версии 1.5–1.6. '
                            'Список products остаётся снаружи; функция его «видит», если он объявлен выше в main.py.'
                        ),
                        'code': (
                            'products = [["яблоко", 52], ["банан", 89], ["хлеб", 265]]\n\n'
                            'def show_menu():\n'
                            '    print("Меню:")\n'
                            '    for i in range(len(products)):\n'
                            '        print(i + 1, ".", products[i][0])\n\n'
                            'show_menu()'
                        ),
                    },
                    {
                        'title': 'Несколько функций — разные задачи',
                        'body': (
                            'Удобно разделить программу на части с понятными именами:\n'
                            '• show_welcome() — приветствие\n'
                            '• show_menu() — список продуктов\n'
                            '• show_journal() — что уже в eaten\n\n'
                            'Каждая функция отвечает за один понятный кусок экрана.'
                        ),
                        'code': (
                            'def show_welcome():\n'
                            '    print("Калькулятор калорий")\n\n'
                            'def show_journal():\n'
                            '    print("Записей:", len(eaten))\n\n'
                            'show_welcome()\n'
                            'show_journal()'
                        ),
                    },
                    {
                        'title': 'Где писать def в файле',
                        'body': (
                            'Функции обычно описывают в начале main.py — после списков products и eaten, '
                            'но до while True. '
                            'Сначала «рецепты» (def), потом основная программа с вызовами. '
                            'Python должен уже знать имя функции к моменту вызова.'
                        ),
                        'code': (
                            'products = [["яблоко", 52]]\n'
                            'eaten = []\n\n'
                            'def show_menu():\n'
                            '    print("Меню...")\n\n'
                            'show_menu()  # вызов после def\n'
                            'while True:\n'
                            '    break'
                        ),
                    },
                    {
                        'title': 'Версия 1.7 — Вывод в функциях',
                        'body': (
                            'Вынесите в функции хотя бы вывод меню (show_menu) и приветствие или журнал. '
                            'Расчёт порции и while True пока можно оставить в основном коде — '
                            'в теме 8 добавим параметры и return для расчёта.'
                        ),
                        'code': (
                            'def show_menu():\n'
                            '    for i in range(len(products)):\n'
                            '        print(i + 1, ".", products[i][0])\n\n'
                            'show_menu()\n'
                            '# while True — как раньше, между порциями можно снова show_menu()'
                        ),
                    },
                ],
            },
        ],
        'remember': [
            'def имя(): — объявление функции, двоеточие обязательно.',
            'Тело функции — строки с отступом под def.',
            'имя() — вызов функции, без вызова тело не выполняется.',
            'Функции описывают до первого вызова — обычно в начале файла после данных.',
            'Одна функция — одна понятная задача: меню, приветствие, журнал.',
        ],
        'mistakes': [
            'Забыть скобки при вызове: show_menu вместо show_menu().',
            'Написать def show_menu без двоеточия — синтaxическая ошибка.',
            'Сбить отступы в теле функции — Python «выпустит» строки из функции.',
            'Вызывать функцию до def — имя ещё не существует.',
            'Копировать def внутрь while, хотя достаточно одного описания снаружи.',
        ],
        'tips': [
            'Давайте функциям глагол в имени: show_menu, print_welcome, show_journal.',
            'После написания def сразу проверьте один вызов в конце файла.',
            'Если функция длинная — возможно, её стоит разбить на две.',
            'Комментарий # меню над def помогает ориентироваться в main.py.',
        ],
    },
    'tasks': [
        {
            'id': 410,
            'kind': 'quiz',
            'category': 'theory',
            'type': 'quiz',
            'text': 'Что делает строка show_menu() в программе?',
            'hint': 'Имя функции со скобками — это вызов.',
            'xp': 10,
            'options': [
                {'key': 'a', 'label': 'Запускает функцию show_menu — выполняется её тело'},
                {'key': 'b', 'label': 'Создаёт новую переменную show_menu'},
                {'key': 'c', 'label': 'Удаляет функцию из программы'},
            ],
            'correct': 'a',
        },
        {
            'id': 411,
            'kind': 'quiz',
            'category': 'theory',
            'type': 'quiz',
            'text': 'Чем def show_menu(): отличается от show_menu()?',
            'hint': 'def — описание, скобки без def — вызов.',
            'xp': 10,
            'options': [
                {'key': 'a', 'label': 'def — объявление функции, show_menu() — её вызов'},
                {'key': 'b', 'label': 'Это одно и то же'},
                {'key': 'c', 'label': 'def вызывает функцию, show_menu() её создаёт'},
            ],
            'correct': 'a',
        },
        {
            'id': 412,
            'kind': 'output_prediction',
            'category': 'trainer',
            'type': 'quiz',
            'text': (
                'def hi():\n'
                '    print("A")\n'
                'print("B")\n'
                'hi()\n'
                'Что выведет программа?'
            ),
            'hint': 'Сначала выполняется print("B") вне функции, потом hi().',
            'xp': 10,
            'options': [
                {'key': 'a', 'label': 'B\nA'},
                {'key': 'b', 'label': 'A\nB'},
                {'key': 'c', 'label': 'A'},
            ],
            'correct': 'a',
        },
        {
            'id': 413,
            'kind': 'code_input',
            'category': 'practice',
            'type': 'code',
            'text': (
                'Опишите функцию greet() без параметров: она печатает «Привет!». '
                'Вызовите greet() один раз.'
            ),
            'hint': 'def greet(): print("Привет!") затем greet()',
            'xp': 14,
            'expected': 'Привет!',
            'starter_code': '# def greet():\n# greet()\n',
        },
        {
            'id': 414,
            'kind': 'code_input',
            'category': 'practice',
            'type': 'code',
            'text': (
                'Опишите show_title() — выводит «Калькулятор калорий». '
                'Вызовите show_title().'
            ),
            'hint': 'def show_title(): print(...)',
            'xp': 14,
            'expected': 'Калькулятор калорий',
            'starter_code': '',
        },
        {
            'id': 415,
            'kind': 'code_input',
            'category': 'practice',
            'type': 'code',
            'text': (
                'products = [["яблоко", 52], ["банан", 89]]. '
                'В функции show_menu() выведите две строки меню через for i in range(len(products)). '
                'Вызовите show_menu().'
            ),
            'hint': 'print(i + 1, ".", products[i][0]) внутри def',
            'xp': 14,
            'expected': '1. яблоко\n2. банан',
            'starter_code': (
                'products = [["яблоко", 52], ["банан", 89]]\n\n'
                'def show_menu():\n'
                '    pass\n\n'
            ),
        },
        {
            'id': 416,
            'kind': 'practice',
            'category': 'practice',
            'type': 'matching',
            'text': 'Сопоставьте запись и что она означает.',
            'hint': 'def — рецепт, () после имени — запуск.',
            'xp': 12,
            'left': ['def show_menu():', 'show_menu()', 'print("Меню") внутри def'],
            'right': [
                'Вызов функции — выполнить её тело',
                'Объявление функции show_menu',
                'Команда внутри тела функции',
            ],
            'correct_pairs': [1, 0, 2],
        },
        {
            'id': 417,
            'kind': 'fix_error',
            'category': 'trainer',
            'type': 'code',
            'text': (
                'Исправьте код: функция должна вывести «Готово», '
                'но сейчас вызов написан неправильно.'
            ),
            'hint': 'Для вызова нужны скобки: done()',
            'xp': 14,
            'expected': 'Готово',
            'starter_code': (
                'def done():\n'
                '    print("Готово")\n\n'
                'done\n'
            ),
        },
        {
            'id': 418,
            'kind': 'code_input',
            'category': 'practice',
            'type': 'code',
            'text': (
                'eaten = ["яблоко", "банан"]. '
                'Функция show_count() выводит строку «Записей: 2» через len(eaten). '
                'Вызовите show_count().'
            ),
            'hint': 'print("Записей:", len(eaten))',
            'xp': 14,
            'expected': 'Записей: 2',
            'starter_code': (
                'eaten = ["яблоко", "банан"]\n\n'
                'def show_count():\n'
                '    pass\n\n'
            ),
        },
        STAGE_M2_07,
    ],
}
